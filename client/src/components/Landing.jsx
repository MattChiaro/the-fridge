import React from 'react';


const Landing = () => {
    return (
        <div>
              <Button variant="primary" onClick={handleShow}>
        Login
      </Button>
        </div>
    );
}

export default Landing;